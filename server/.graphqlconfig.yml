projects:
  app:
    schemaPath: "src/schema.graphql"
    extensions:
      endpoints: "https://eu1.prisma.sh/tobiall/server/dev"
        default: "http://localhost:4000"
  database:
    schemaPath: "src/generated/prisma.graphql"
    extensions:
      prisma: database/prisma.yml
